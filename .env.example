# Atlassian API Configuration (Required)
ATLASSIAN_DOMAIN=your-domain.atlassian.net
ATLASSIAN_EMAIL=your-email@example.com
ATLASSIAN_API_TOKEN=your-api-token

# Field Filtering Configuration (Optional)
# Comma-separated list of Jira custom fields to include
# JIRA_CUSTOM_FIELDS=customfield_10001,customfield_10002

# Comma-separated list of Confluence expand parameters
# CONFLUENCE_CUSTOM_EXPAND=ancestors,children,history

# Project/Space Filtering Configuration (Optional)
# Comma-separated list of Jira projects to filter by default
# JIRA_PROJECTS_FILTER=TMS,BSRI

# Comma-separated list of Confluence spaces to filter by default
# CONFLUENCE_SPACES_FILTER=COMMON,ProjectTMS

# Performance Configuration (Optional)
# MAX_CONNECTIONS=100
# REQUEST_TIMEOUT_MS=30000

# Logging Configuration (Optional)
LOG_LEVEL=warn